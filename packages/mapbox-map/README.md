# Mapka mapbox map components

```bash
yarn add @mapka/mapbox-map
```

## Usage

```tsx
import { MapboxMap } from "@mapka/mapbox-map";

<MapboxMap accessToken={accessToken} onMapLoaded={(map) => console.log(map)} />
```
