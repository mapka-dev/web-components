# React Mapka leaflet map

Mapka leaflet map package.

```bash
yarn add @mapka/react-leaflet-map
```

## Usage

```tsx
import { LeafletMap } from "@mapka/react-leaflet-map";

<LeafletMap onMapLoaded={(map, L) => console.log({ map, L })} />
```
