# React Mapka mapbox map components

```bash
yarn add @mapka/react-mapbox-map
```

## Usage

```tsx
import { MapboxMap } from "@mapka/react-mapbox-map";

<MapboxMap accessToken={accessToken} onMapLoaded={(map) => console.log(map)} />
```
